using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundAIController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //assign unit to building?
    }

    // Update is called once per frame
    void Update()
    {
        //if unit is assigned to a building, it performs the action associated with that building
    }
}
